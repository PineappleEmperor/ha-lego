# Test-only dependencies. Installed by .github/workflows/python_validate.yml
# before the pytest step; also `pip install -r requirements.test.txt` locally.
#
# pytest-homeassistant-custom-component tracks Home Assistant releases 1:1 and
# hard-pins `homeassistant==<that release>`, so the pin below decides which HA
# version the suite runs against. A mismatched pin fails at import, not at test
# time. Pick the release matching the HA you target, and keep its Python floor
# in lockstep with the `python-version` matrix in python_validate.yml,
# `target-version` in pyproject.toml, and pyrightconfig.json.
#
# Version map (verify at pypi.org/project/pytest-homeassistant-custom-component):
#   0.13.354 -> homeassistant 2026.8.0, requires-python >=3.14
pytest-homeassistant-custom-component==0.13.361

# Add integration-specific test deps below (e.g. a fake transport, respx,
# freezegun beyond what the plugin already pins).
markdown-it-py
ruff==0.16.5
pyright==1.1.411

# The panel makes `frontend` a manifest dependency, and the frontend component
# has its own pip requirement that installing homeassistant does not pull in.
# Without it every setup test fails with "No module named 'hass_frontend'".
# Read the pin from homeassistant/components/frontend/manifest.json whenever the
# pytest-homeassistant-custom-component pin above moves.
home-assistant-frontend==20260826.1
